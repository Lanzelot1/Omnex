"""Setup configuration for Omnex."""

from setuptools import setup

# The actual configuration is in pyproject.toml
# This file exists for compatibility with older tools

if __name__ == "__main__":
    setup()